<%if Producto.last%>
	<%= form_for(@pedido) do |f| %>
	<h1>Registrar nuevo pedido</h1>
	<% @pedido.errors.full_messages.each do |message|%>
		<div class="letter-red"><%=message%></div>
		<p></p>
		<%end%>
	<div>Nombre cliente:</div>
	<div class="field">
		<%= f.text_field :cliente, placeholder: "Nombre cliente" %>
	</div>

	<p></p>
	<div>Fecha de entrega:</div>
	<div class="field">
	<%=f.text_field :entrega, placeholder: "AAAA-MM-DD"%></div>

	</br	><div>Rut(sin puntos):</div>
	<div class="field">
		<%= f.text_field :rut, placeholder: "11222333", :maxlength=>"8" %>
	<%="  -  "%>
	<class="field" size-80">
		<%= f.text_field :digito, placeholder: "1" , :maxlength=>"1" %>
	</div></br>
	<p></p>
	<div>Comentario:</div>
	<div class="field">
		<%= f.text_area :comentario, placeholder: "Comentario" %>
	</div>

	<p></p>
	<div class="field">
		<%= f.submit "Guardar" %>
	</div>
	<P></P>
	<input type="button" value="Volver" onClick="history.back()" />
	<%end%>
	</br>
	</br>
	</br>
	</br>
	</br>
	</br>
	</br>
<%else%>
	<h3 class="be-re">No se han ingresado productos al sistema. Conmuníquese con administrador</h3>
<%end%>

	 