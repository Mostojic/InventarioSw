<%if Pedido.find_by estado: 2%>
<h1>Pedidos entregados</h1>
<div class="container">
      <div class="col-md-6">
        <table class="table table-striped table-hover table-condensed   " >
          <thead>
            <tr>
              <th><div class="size-330">Cliente</div></th>
              <th><div class="size-165">Costo total</div></th>
              <th><div class="size-165">Fecha entrega</div></th>
              <th><div class="size-165"></div></th>
            </tr>                  
          </thead>
          
          <% @pedidos_entregado.each do |a| %>
          
            <tr>
              <td><%=a.cliente%></td>
              <td><%=a.precio%></td>
              <td><%=a.entrega%></td>
              <td><button type="button"  onClick="location.href='listas/<%= a.id%>'" />Detalles</button></td>
              
            </tr>
          
          <%end%>
        </table>
        <%=will_paginate @pedidos_entregado%>
        </div>
      </div>
<%else%>
<h1>No existem pedidos pendientes</h1>
<%end%>

<input type="button" value="Volver" onClick="history.back()" />

